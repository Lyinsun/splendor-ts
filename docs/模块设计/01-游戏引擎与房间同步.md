# 游戏引擎与房间同步

## 概述

游戏引擎负责结算 Splendor-style 行动并产出权威 `GameState`；房间同步负责把该状态广播给所有连接客户端。两者边界清晰：同步层只传输，不决定规则。

## 架构图

```mermaid
flowchart TD
    A["HTTP DTO"] --> B["RoomService"]
    B --> C["GameAction"]
    C --> D["Domain Engine"]
    D --> E["GameState"]
    E --> F["HTTP Response"]
    E --> G["WebSocket room_state"]
```

## 领域模型

```mermaid
classDiagram
    class GameState {
        +roomId: string
        +status: GameStatus
        +players: PlayerState[]
        +board: BoardState
        +currentPlayerId: string
        +winnerIds: string[]
    }
    class PlayerState {
        +tokens: TokenBank
        +bonuses: ElementCounter
        +tableau: CompanionCard[]
        +reserved: CompanionCard[]
        +score: number
    }
    class BoardState {
        +bank: TokenBank
        +market: Record<CardTier, CompanionCard[]>
        +gymLeaders: GymLeader[]
    }
    class GameAction {
        +kind: take_tokens | reserve_card | buy_card
        +playerId: string
    }
    GameState --> PlayerState
    GameState --> BoardState
    BoardState --> CompanionCard
    BoardState --> GymLeader
```

## 核心流程

```mermaid
sequenceDiagram
    participant Client
    participant RoomService
    participant Engine
    participant Hub

    Client->>RoomService: create/join/start/action
    RoomService->>Engine: validate and settle
    Engine-->>RoomService: next GameState
    RoomService->>Hub: emit snapshot
    Hub-->>Client: room_state
```

## 接口与契约

- `createLobbyState`: 创建 lobby 状态。
- `addPlayerToLobby`: lobby 期加入玩家。
- `startGame`: 初始化银行、市场、导师和回合。
- `applyGameAction`: 唯一行动结算入口。
- `RoomService`: 管理内存房间、调用 domain、触发广播。
- `RoomWebSocketHub`: 订阅 `RoomService` 并广播 `room_state`。

## 设计决策与约束

- MVP 使用内存房间。未来持久化应新增 repository/adapter，不改变 domain 规则入口。
- `RoomService` 返回 `structuredClone` 快照，避免调用方持有可变内部状态。
- 10-token 上限在 MVP 中通过拒绝行动处理。
